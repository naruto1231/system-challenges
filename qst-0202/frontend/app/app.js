
const URL_BACKEND = 'http://localhost:4000';


/**
 * 郵便番号で住所を検索する
 */
const searchByZipCode = async () => {
    const inputtedValue = document.getElementById('input-zip').value;
    // ここに郵便番号検索の処理を書く
}

/**
 * 住所で郵便番号を検索する
 */
const searchByAddress = async () => {
    const inputtedValue = document.getElementById('input-address').value;
    // ここに住所検索の処理を書く
}